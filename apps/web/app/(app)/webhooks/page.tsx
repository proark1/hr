import { ComingSoon } from "@/components/coming-soon";
export default function Page() {
  return <ComingSoon title="Webhooks" body="Subscribe to employee.* / leave.* / contract.* events. Endpoint registration UI shipping with the webhook delivery PR." />;
}
